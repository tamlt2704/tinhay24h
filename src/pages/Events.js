import { Container } from "react-bootstrap";
import Body from "../components/Body";

export default function Events() {
    return (
        <Container>
            <Body pageType='events'/>
        </Container>
    )    
}